# ABQ's EACF — Agent Instructions for OpenAI Codex

> All agent logic lives in `AGENTS_BOOT.md`. Codex loads it automatically via `.codex/config.toml`
> (`project_doc_fallback_filenames = ["AGENTS_BOOT.md"]`).
> This file is kept as the primary AGENTS.md entry point — it loads before AGENTS_BOOT.md.

Before doing any work, read `AGENTS_BOOT.md` in this repository root. It contains your full
operating instructions as an EACF-configured agent. Apply everything in it.
